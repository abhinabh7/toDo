services:
  web:
    build: .
    image: abhinabh/todo-app:latest
    ports:
      - "5000:5000"
    volumes:
      - todo_data:/app/instance
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  todo_data: